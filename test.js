earhorn$.flushInterval = 0 // Make the demo more responsive

earhorn$(this, 'mouse.js', function() {

  var x, y

  $(parent.document).on('mousemove', function(evt) {
    
    // Mouse movement tracked in real-time
    
    evt
    
    x = evt.clientX    
    y = evt.clientY
    
  })
})

